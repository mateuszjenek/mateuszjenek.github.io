import { Profile } from "../../domain/profile";

/**
 * View model for Profile presentation.
 * Currently a pass-through, but can be extended for presentation-specific transformations.
 */
export type ProfileViewModel = Profile;

